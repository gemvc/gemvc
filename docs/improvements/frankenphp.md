# FrankenPHP support (plan of record)

**Status:** Classic mode **implemented** in library (startup templates + `InitFrankenPHP` + detector + docs).  
**Guide:** [../guides/frankenphp.md](../guides/frankenphp.md)

| Item | Status |
|------|--------|
| Classic mode (`ApacheRequest` + `Bootstrap` + PDO) | Shipped |
| `Caddyfile` path denies (no `.htaccess`) | Shipped |
| `gemvc init --frankenphp` | Shipped |
| Worker mode | Planned follow-up (see openswoole isolation lessons) |
| gRPC | Out of scope |

Backlog index: [README.md](README.md).
